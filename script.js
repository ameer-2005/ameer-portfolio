const words=['Computer Science Student','Java Developer','Machine Learning Enthusiast','Web Developer'];let wi=0,ci=0,del=false;const typing=document.querySelector('#typing');function type(){const w=words[wi];typing.textContent=w.slice(0,ci);if(!del&&ci<w.length)ci++;else if(!del){del=true;setTimeout(type,1500);return}else if(del&&ci>0)ci--;else{del=false;wi=(wi+1)%words.length}setTimeout(type,del?38:72)}type();
const progress=document.querySelector('.progress');addEventListener('scroll',()=>{progress.style.width=`${scrollY/(document.documentElement.scrollHeight-innerHeight)*100}%`});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
const glow=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
document.querySelector('.menu').onclick=()=>document.querySelector('.nav-links').classList.toggle('open');document.querySelectorAll('.nav-links a').forEach(a=>a.onclick=()=>document.querySelector('.nav-links').classList.remove('open'));
function sendMessage(e){e.preventDefault();const f=e.target;const name=f.querySelector('input').value;location.href=`mailto:ameerunnisa019@gmail.com?subject=Portfolio enquiry from ${encodeURIComponent(name)}&body=${encodeURIComponent(f.querySelector('textarea').value)}`}
const c=document.querySelector('#particles'),x=c.getContext('2d');let p=[];function resize(){c.width=innerWidth;c.height=innerHeight;p=Array.from({length:55},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*1.3+.2,v:Math.random()*.23+.05}))}function draw(){x.clearRect(0,0,c.width,c.height);x.fillStyle='#7aa8ff';p.forEach(a=>{a.y-=a.v;if(a.y<0)a.y=c.height;x.globalAlpha=.22;x.beginPath();x.arc(a.x,a.y,a.r,0,7);x.fill()});requestAnimationFrame(draw)}resize();draw();addEventListener('resize',resize);
